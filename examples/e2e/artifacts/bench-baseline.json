{
  "_purpose": "Curated CI regression gate for Task 13's benchmark, fix round 1, sensitivity corrected in fix round 2. Committed deliberately and NOT regenerated by bench.spec.ts on every run (see examples/e2e/bench.spec.ts) -- a baseline that updates itself every run could never catch a regression. frameWorkTimeP95Ms is the highest p95 observed across 9 independent baseline runs (3 Playwright process invocations x 3 in-process page loads each) at entityCount=500. toleranceMultiplier x frameWorkTimeP95Ms = 1.6ms, which is 5.3x the typical observed p95 (~0.30ms) -- NOT '>10x' as an earlier draft of this file claimed. ACTUAL SENSITIVITY, worked out from this benchmark's own numbers: the entity-attributable portion of frameWorkTime is roughly 0.15-0.20ms (500-entity mean ~0.19-0.25ms minus the 0-entity control's ~0.06-0.07ms floor, both established in bench.spec.ts's null-hypothesis control). For p95 to cross 1.6ms, that entity-attributable cost has to grow by roughly 8-12x -- NOT the 10x place()-cost or broken-depthSort-coalescing scenarios an earlier draft named as examples, neither of which actually trips this gate at today's numbers (a 10x place() regression adds only ~0.15-0.5ms, landing p95 near 0.5-0.9ms; a second same-frame sort adds ~0.03-0.05ms and would be caught by the depthSort===1 assertion instead, well before this gate would ever see it). This gate is a catastrophe-catcher -- an 8-12x blowup in entity-attributable cost -- not a fine-grained regression detector, and that is a legitimate but different claim than 'catches place() costing 10x more'. It is NOT the 16.6ms/60fps aspirational threshold from the brief -- that comparison stays informational-only in the report, per the task's own rule against gating CI on an aspirational number.",
  "recordedOn": "2026-07-28",
  "machine": "Apple M1 Max, 10 CPU cores, 64GB RAM, macOS 26.5.2, headless Chromium via Playwright 1.62.0, WebGL through SwiftShader (software, confirmed via WEBGL_debug_renderer_info) -- NOT a CI runner.",
  "entityCount": 500,
  "frameWorkTimeP95Ms": 0.4,
  "toleranceMultiplier": 4
}
