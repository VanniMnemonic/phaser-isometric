{
  "name": "examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "4.2.1",
    "phaser-isometric": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
